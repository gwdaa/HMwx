// 配置项
const host = 'https://yzex9nc5.qcloud.la'
// const host = 'https://localhost:5757'

const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}
export default config
